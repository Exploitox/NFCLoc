<Window x:Class="ZeroKey.GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ZeroKey.GUI"
        xmlns:pages="clr-namespace:ZeroKey.GUI.Pages"
        mc:Ignorable="d"
        Title="Codename ZeroKey"
        Height="550"
        Width="850"
        MinHeight="550"
        MinWidth="850"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        xmlns:wpfui="clr-namespace:WPFUI.Controls;assembly=WPFUI"
        Style="{StaticResource UiWindow}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="1" Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <wpfui:NavigationStore
                x:Name="RootNavigation"
                Grid.Column="0"
                Margin="6,0,6,0"
                Frame="{Binding ElementName=RootFrame}"
                Loaded="RootNavigation_OnLoaded">
                <wpfui:NavigationStore.Items>
                    <wpfui:NavigationItem
                        Content="Add"
                        Icon="Add24"
                        Tag="add"
                        Page="{x:Type pages:AddCard}"/>
                    <wpfui:NavigationItem
                        Content="Manage"
                        Icon="ContactCard24"
                        Tag="manage"
                        Page="{x:Type pages:ManageCards}" />
                </wpfui:NavigationStore.Items>
                <wpfui:NavigationStore.Footer>
                    <wpfui:NavigationItem
                        Content="About"
                        Icon="QuestionCircle24"
                        Tag="about"
                        Page="{x:Type pages:About}" Cursor="Hand" />
                </wpfui:NavigationStore.Footer>
            </wpfui:NavigationStore>

            <Border
                Grid.Column="1"
                Background="{DynamicResource ControlFillColorDefaultBrush}"
                CornerRadius="8,0,0,0">
                <Grid>
                    <Frame x:Name="RootFrame" />
                    <wpfui:Breadcrumb
                        Margin="18,18,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        FontSize="24"
                        Navigation="{Binding ElementName=RootNavigation}" />
                </Grid>
            </Border>
        </Grid>

        <Label x:Name="debugLabel" Grid.Row="0" Content="" Margin="90,17,0,0"/>
        <wpfui:TitleBar Grid.Row="0" ShowMaximize="False" ShowMinimize="False" Title="ZeroKey" Icon="pack://application:,,,/Icon.ico"/>
    </Grid>
</Window>