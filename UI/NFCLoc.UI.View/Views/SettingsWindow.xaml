<Window x:Class="NFCLoc.UI.View.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NFCLoc.UI.View.Views"
        mc:Ignorable="d"
        WindowStyle="ToolWindow"
        WindowStartupLocation="CenterScreen"
        Title="NFCLoc - Settings" Height="420" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Image 
            Source="/NFCLoc.UI.View;component/Assets/Wolkenhof-Logo.png" Stretch="Uniform"
            Margin="130,10,130,0"/>

        <Grid Grid.Row="1" Margin="10" ShowGridLines="false">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="240"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>

            <StackPanel>
                <StackPanel.Resources>
                    <Style TargetType="{x:Type CheckBox}">
                        <Setter Property="Margin" Value="0,10,0,0"/>
                    </Style>
                </StackPanel.Resources>

                <CheckBox Content="Show on startup"/> 
                <GroupBox Header="Authentication Server Information" Margin="0,10,0,0">
                    <StackPanel>
                        <CheckBox x:Name="a" Content="Use Authentication Server" Margin="0,10,0,0"/>
                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                FontSize="12"
                                Margin="0,0,0,0"
                                Text="IP Address:" />
                            <TextBox x:Name="TbIp" Grid.Column="1" IsEnabled="false" />
                        </Grid>
                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                FontSize="12"
                                Margin="0,0,0,0"
                                Text="Username:" />
                            <TextBox x:Name="TbUser" Grid.Column="1" IsEnabled="false" />
                        </Grid>
                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                FontSize="12"
                                Margin="0,0,0,0"
                                Text="Password:" />
                            <TextBox x:Name="TbPW" Grid.Column="1" IsEnabled="false" />
                        </Grid>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
